rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 클라이언트 접근 차단 — 데이터는 반드시 백엔드(Admin SDK) 경유
    // KIS app_key / app_secret / access_token 등 민감 정보 보호
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
